import Testing
@testable import Seedkeep

@Suite("Seedkeep app target placeholder")
struct SeedkeepTests {
    @Test func packageReachable() {
        // Placeholder so the test target has at least one symbol.
        // Real app-level tests live here once C-ios lands views to drive.
        #expect(true)
    }
}

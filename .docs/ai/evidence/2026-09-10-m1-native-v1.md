# Milestone 1 native V1 evidence — 2026-09-10

This report records the iOS-side work for `seedkeep-27d.33`. Build 54 remains
the current source candidate (`1.0.0/54`); no build counter, archive,
TestFlight/build upload, or release metadata was changed.

## Source and tests

`RecommendationStore` now has focused contract coverage through the existing
`SeedkeepClient.Configuration` URLSession injection and
the suite-local `RecommendationRouterMockURLProtocol`. The tests cover the single recommendation GET
request and DTO persistence, fetch-then-upsert replacement, pending IDs carried
into the next bulk request, `no_household_location` state without partial
persistence, and server errors preserving a cached row.

The controlled fake-engine coverage already present in
`HouseholdCloudCoordinatorTests` remains the source proof for missing-photo
isolation and durable permanent-photo failure across relaunch with explicit
retry. The date regression coverage remains in `BugBashFixTests` for Journal
roundel/accessibility and planting-event presentation in America/Chicago and
UTC.

The durable source audit is anchored in `Seedkeep/App/AppEnvironment.swift:49-51`
(the five AppTabs), `Seedkeep/Features/MainTabView.swift:4-11` (root mounts
and deferred Sprout/MCP surfaces), `Seedkeep/Core/Preferences/AppPreferences.swift:16-20,73-77`
(Hosted gate and stored-tier fallback), `Seedkeep/Core/FeatureFlags.swift:7-15`
(pets gate), and `Seedkeep/App/SeedkeepApp.swift:68-89` (reachable invite and
deletion handoff links). These files show every V1 tab and core secondary
surface present while Sprout/MCP, Hosted, and pets remain unreachable. This is
source evidence only and does not replace live-device execution evidence.

The prepared successor changelog notes are: photo uploads retain a durable
failure roster with explicit retry, recommendation requests persist and
refresh catalog-linked planting guidance, and date-only garden/journal
displays remain stable across Chicago and UTC. `ChangelogData` now contains a
newest-first full `1.0.0` build-55 entry carrying the public build-54 content
plus the planting-date repair, while retaining the rejected build-54 entry.

The build-55 release-integrity check is intentionally non-mutating. It proves
the release script plans `1.0.0 (54) -> 1.0.0 (55)`, leaves `project.yml`
unchanged, and confirms the tracked project.yml counter remains 54 alongside
both changelog entries.

## Commands and logs

`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer ./scripts/release.sh --build --plan-version`
returned `1.0.0 (54) -> 1.0.0 (55)`. A before/after SHA-256 check of
`project.yml`, together with direct checks of the tracked project counter and
both changelog entries, confirmed that planning left build-54 metadata
unchanged.

The full gate command was
`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer ./scripts/test-gate.sh --simulator-udid 296B556A-C784-4C92-8534-B2BE4F7FEC07`
with output captured in `/tmp/seedkeep-m1-test-gate.log`. It passed 97
SeedkeepKit tests, 148 SeedkeepCloudKit tests, 769 CloudKit-OFF app tests, and
the one-test Production-default CloudKit-ON contract. The CloudKit-OFF app
result bundle is
`~/Library/Developer/Xcode/DerivedData/Seedkeep-gsmxhdgazbttfzesevipcfdjdrtu/Logs/Test/Test-Seedkeep-2026.09.10_10-09-09--0500.xcresult`;
the Production-default result bundle is
`~/Library/Developer/Xcode/DerivedData/Seedkeep-gsmxhdgazbttfzesevipcfdjdrtu/Logs/Test/Test-Seedkeep-2026.09.10_10-09-43--0500.xcresult`.
This full gate preceded the final suite-local fixture and build-55 changelog
corrections; no final full-gate rerun is claimed.

The focused RecommendationStore command was the same Xcode destination with
`-only-testing:SeedkeepTests/RecommendationStoreTests`; all five new Swift
Testing cases passed, with the isolated suite log at
`/tmp/seedkeep-m1-recommendation-tests.log` and result bundle
`~/Library/Developer/Xcode/DerivedData/Seedkeep-gsmxhdgazbttfzesevipcfdjdrtu/Logs/Test/Test-Seedkeep-2026.09.10_10-17-32--0500.xcresult`.

The impacted changelog command used
`-only-testing:SeedkeepTests/ChangelogDataTests` and passed all three tests;
its output is `/tmp/seedkeep-m1-changelog-test.log` and its result bundle is
`~/Library/Developer/Xcode/DerivedData/Seedkeep-gsmxhdgazbttfzesevipcfdjdrtu/Logs/Test/Test-Seedkeep-2026.09.10_10-18-42--0500.xcresult`.

## Physical-device and controlled-retry gate

The designated iPad simulator `B84C7F39-7F0C-425E-B17B-033259EB83A8` is now
authenticated and reachable in the five-tab app. It exercised the real Seed
Detail and Journal Entry “Retry photo sync” controls against a temporary
M1-labelled failure roster. The exact setup, record names, UI result, CloudKit
operation IDs, and restoration are recorded in
`.docs/ai/evidence/2026-09-10-m1-live-retry-log.txt`.

CloudKit Production logs show matching asset transfer attempts for the seed
photo (MMCS fixed 87,110 bytes) and journal photo (MMCS fixed 174,220 bytes),
but both saves returned `Server Record Changed` because these records already
exist. The roster cleared after the UI actions and the original synced-state
entries were restored. This is useful failure/retry-path evidence, but it does
not prove a newly accepted upload. A fresh supported Add Photo check reached
the native PhotosPicker: XcodeBuildMCP runtime snapshot sequence 25 exposed its
`e160|tap|button|Cancel` target and `Loading...` text, and the simulator log
showed the photo-picker scene launch and readiness. The remote picker scene was not exposed to the XcodeBuildMCP accessibility tree.
A final native CUA Simulator screenshot did show the picker visually, including
the known synthetic barcode as the first grid tile, but the native click API
requires an accessibility `elementIndex`; its coordinate call was rejected
before interaction. No known synthetic item could therefore be selected and no
new photo upload acknowledgment was obtained. This is a tooling/session
visibility boundary, not a diagnosed app wiring defect.
The source wiring is directly traceable at
`Seedkeep/Features/SeedDetail/SeedDetailView.swift:276-299` (PhotosPicker
selection/change handler) and `:302-324` (transfer, resize, and
`uploadPhoto` call).

Separately, the supported Add Seed form created `M1 Retry Scalar QA 2`; runtime
snapshot sequence 57 showed the new item and Active count 2. Production CloudKit
acknowledged its new `Seed` record with `savedRecords=[...]` and
`failedRecordSaves=[]` (operation `173E89E24AD7137B`). This proves sender/server
scalar save only; no second receiver was available to prove convergence.

`xcrun devicectl list devices` found the paired iPhone 15 Pro `Roshar`
(`A885F93A-60DD-59DA-9049-289C35EACE23`) available, and the connected iPad Air
`Sel` (`3212F155-8037-5310-B333-F59018F2AD78`). The supervised live
recommendation proof still requires Roshar Camera to scan the prepared online
barcode, record the raw detected value, save the live catalog hit, configure a
Home ZIP, and confirm Seed Detail planting guidance. Simulator import and
fixture data do not satisfy this gate; the barcode card is
`.docs/ai/evidence/2026-09-10-m1-online-barcode-card.html`.

## V1 capability evidence matrix

| Capability | Evidence and boundary |
| --- | --- |
| Seed inventory and scanning | Five-tab/root source anchors above; prior simulator scan/inventory reachability is retained in the M1 handoff. Roshar live barcode → catalog → save remains open. |
| Garden planning and planting dates | `BugBashFixTests` Chicago/UTC date suites plus prior Garden/Bed Detail runtime evidence; current iPad reachability after retry was recorded, without treating it as a fresh date regression run. |
| Journal, checklists, and photos | `HouseholdCloudCoordinatorTests` fake-engine failure/isolation coverage; current iPad Journal Entry and both Retry controls were exercised. New accepted Production photo upload remains open. |
| Weather and planting guidance | Existing notification/weather suites in the full gate and source reachability; live recommendation depends on Roshar catalog resolution and Home ZIP. |
| iCloud sharing | Invite/deletion handoff is reachable from `SeedkeepApp`; cross-account asset delivery and shared-owner transfer remain the explicit `seedkeep-cko.4` / `seedkeep-27d.25` waiver. |
| Account deletion | Deletion handoff source path and account-deletion suites in the full gate; no destructive live deletion was performed. |

The matrix distinguishes source, test, prior runtime, current retry runtime,
and the still-open physical recommendation/upload gates. It does not claim
that every CRUD or settings path was physically re-exercised in M1.

## Release boundaries

No archive, TestFlight/build upload, build bump, server/schema/site mutation,
App Store submission, external-provider dispatch, or deletion of pre-existing
data was performed. Production asset-transfer attempts and the synthetic scalar
save are recorded above; neither substitutes for an accepted fresh photo
upload or second-receiver convergence. The existing single-account waiver for cross-account asset delivery
and shared-owner transfer remains in force.
